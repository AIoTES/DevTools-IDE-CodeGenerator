{
@context:{				
	
	#foreach($class in $ontology.getClassesInSignature())
		"${class.getIRI.getFragment()}":"${class.getIRI()}",
		#foreach($domain in $ontology.getAxioms($axiomtype.DATA_PROPERTY_DOMAIN))
			#if($domain.getClassesInSignature().contains($class))
				
			#end
		#end
	#end
			
	}
}

	