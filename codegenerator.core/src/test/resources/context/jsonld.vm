{
@context:{				
#foreach($axiom in $ontology.getAxioms($axiomtype.DATA_PROPERTY_RANGE))
	#foreach($entity in $axiom.getSignature())
		#foreach( $element in $entity.getSignature())
			#foreach($item in $element.getSignature())
"${item.getIRI()}":"${item.getIRI().getFragment()}",
			#end
		#end
	#end
#end	
			
	}
}

	